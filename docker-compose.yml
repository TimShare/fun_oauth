services:
  fastapi-oauth:
    build: .
    container_name: fastapi-oauth-app
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - HOST=0.0.0.0
      - PORT=8000
    volumes:
      - .:/app
    restart: unless-stopped
    command: python main.py
